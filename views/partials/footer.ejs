<footer class="text-gray-600 body-font">
  <div class="container px-5 py-8 mx-auto flex items-center sm:flex-row flex-col">
    <a class="flex title-font font-medium items-center md:justify-start justify-center text-gray-900">
   <img src="<%= logo %>" alt="logo" class="dlogo  h-15 w-28">
    </a>
    <p class="text-sm text-gray-500 sm:ml-4 sm:pl-4 sm:border-l-2 sm:border-gray-200 sm:py-2 sm:mt-0 mt-4">© 2024 Netxsites —
      <a href="https://netxsites.com" class="text-gray-600 ml-1" rel="noopener noreferrer" target="_blank">@connect_with_us</a>
    </p>
    <span class="inline-flex sm:ml-auto sm:mt-0 mt-4 justify-center sm:justify-start">
      <a class="text-gray-500">
        <svg fill="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="w-5 h-5" viewBox="0 0 24 24">
          <path d="M18 2h-3a5 5 0 00-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 011-1h3z"></path>
        </svg>
      </a>
      <a class="ml-3 text-gray-500">
        <svg fill="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="w-5 h-5" viewBox="0 0 24 24">
          <path d="M23 3a10.9 10.9 0 01-3.14 1.53 4.48 4.48 0 00-7.86 3v1A10.66 10.66 0 013 4s-4 9 5 13a11.64 11.64 0 01-7 2c9 5 20 0 20-11.5a4.5 4.5 0 00-.08-.83A7.72 7.72 0 0023 3z"></path>
        </svg>
      </a>
      <a class="ml-3 text-gray-500">
        <svg fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="w-5 h-5" viewBox="0 0 24 24">
          <rect width="20" height="20" x="2" y="2" rx="5" ry="5"></rect>
          <path d="M16 11.37A4 4 0 1112.63 8 4 4 0 0116 11.37zm1.5-4.87h.01"></path>
        </svg>
      </a>
      <a class="ml-3 text-gray-500">
        <svg fill="currentColor" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="0" class="w-5 h-5" viewBox="0 0 24 24">
          <path stroke="none" d="M16 8a6 6 0 016 6v7h-4v-7a2 2 0 00-2-2 2 2 0 00-2 2v7h-4v-7a6 6 0 016-6zM2 9h4v12H2z"></path>
          <circle cx="4" cy="4" r="2" stroke="none"></circle>
        </svg>
      </a>
    </span>
  </div>
</footer>
</div>

</body>

<script>


  
const setTheme = () => {
    const html = document.documentElement;
    const toggler = document.querySelector('.toggler');
    if (localStorage.getItem('color-theme') === 'dark' || (!localStorage.getItem('color-theme') && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        html.classList.add('dark');
        toggler.classList.add('dark');
        toggler.children[0].classList.add('translate-x-10');
    } else {
        html.classList.remove('dark');
        toggler.classList.remove('dark');
    }
};

// Toggle Dark Function
const toggleDark = () => {
    const html = document.documentElement;
    const toggler = document.querySelector('.toggler');

    toggler.addEventListener('click', () => {
        toggler.children[0].classList.toggle('translate-x-10');
        html.classList.toggle('dark');
        toggler.classList.toggle('dark');
        if (html.classList.contains('dark')) {
            localStorage.setItem('color-theme', 'dark');
        } else {
            localStorage.setItem('color-theme', 'light');
        }
    });
};

    setTheme();
    toggleDark();


    document.addEventListener('DOMContentLoaded', function() {
        var menuButton = document.getElementById('menu-button');
        var sidebar = document.getElementById('sidebar');

        menuButton.addEventListener('click', function() {
            sidebar.classList.toggle('hidden');
            sidebar.classList.toggle('lg:block');
        });
    });
    
function appplyvisa(e){

    var companyname = $(e).attr('data-cname');
        var compphone = $(e).attr('data-cph');
            var companyemail = $(e).attr('data-cem');
                        var serv = $(e).attr('data-serv');
                        var companyauth = $(e).attr('data-auth');
                        var belongs_to=$(e).attr('data-bto');
                        
 
    var iframeContent =`<iframe id="oDYiswiBsdEN3kPpiZ4k" src="https://api.leadconnectorhq.com/widget/survey/oDYiswiBsdEN3kPpiZ4k?organization_phone=${compphone}&organization_name=${companyname}&organization_email=${companyemail}&authority=${companyauth}&service=${serv}&tag=${serv}&belongs_to=${belongs_to}" style="width:100%;height:100vh" allowfullscreen>Loading</iframe>`;
    
    
        Swal.fire({
        title: "Apply Visa For "+ companyname,
        html: iframeContent, // Use "html" instead of "content.element"
        showCloseButton: true, // Optionally show a close button
        focusConfirm: false, // Prevent focusing on the confirm button
        showConfirmButton: false, 
        
        customClass: {
            container: 'custom-swal-container' // Add custom class for styling
        }
    });
    
    
}    
    
function showFolder(e) {
    var title = $(e).attr('title');
    var urls = $(e).attr('data').split(',');
    var iframeContent = '';

    urls.forEach(function(url) {
        iframeContent += `
            <div style="position: relative;display:inline-block">
                <iframe  scrolling="no" seamless src="${url}" style="width: 150px; height: 150px;margin:10px; border: none;overflow:hidden;border: solid thin lightgray;
    border-radius: 10px;"></iframe>
                <a href="${url}" target="_blank" download class="download-button" style="position: absolute; bottom: 10px; right: 10px; padding: 5px 10px; background-color: #007bff; color: #fff; text-decoration: none; border-radius: 5px;">Download</a>
            </div>
        `;
    });

    Swal.fire({
        title: title,
        html: iframeContent, // Use "html" instead of "content.element"
        showCloseButton: true, // Optionally show a close button
        focusConfirm: false, // Prevent focusing on the confirm button
        customClass: {
            container: 'custom-swal-container' // Add custom class for styling
        }
    });
}


</script>
<style>
    .dark{
        /* background-color: black; */
        color:white
    }
    .dlogo:is(.dark *) {
    filter: brightness(0) invert(1) !important;
    }
    
    
    
    
    
/* Scrollbar Styling */
::-webkit-scrollbar {
    width: 5px;
}
 
::-webkit-scrollbar-track {
    background-color: #ebebeb;
    -webkit-border-radius: 10px;
    border-radius: 10px;
}

::-webkit-scrollbar-thumb {
    -webkit-border-radius: 10px;
    border-radius: 10px;
    background: #6d6d6d; 
}
    
    
    
</style>

</html>